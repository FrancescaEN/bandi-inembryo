avvia il server con:
$uvicorn main:app --reload

per swagger: http://127.0.0.1:8000/docs
salva i bandi nel db -> POST /scrape
legge i bandi -> GET /bandi

dopo:
PostgreSQL (prod)
sistema newsletter automatico
matching intelligente bandi ↔ utenti
